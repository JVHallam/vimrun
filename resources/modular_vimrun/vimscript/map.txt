"           ( map )

"#Create a function that
"* Takes an array
"* returns a copy of that array that
"* has each value doubled
"* without mutating the state of the original array
"* let a = map( x, func ), the let a is required for the thing to work
